/**
 * Legacy CommonJS profile service tests were migrated to
 * `profileService.test.ts`. This placeholder keeps Jest globs intact while
 * avoiding duplicate coverage.
 */

describe.skip('profileService legacy suite', () => {
  it('migrated to TypeScript test harness', () => {
    expect(true).toBe(true);
  });
});
