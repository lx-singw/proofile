/**
 * Legacy CreateProfileForm tests migrated to TypeScript in
 * `CreateProfileForm.test.tsx`. This file remains to prevent breaking
 * existing Jest globs; the suite is intentionally skipped.
 */

describe.skip('CreateProfileForm legacy suite', () => {
  it('migrated to TypeScript test harness', () => {
    expect(true).toBe(true);
  });
});
